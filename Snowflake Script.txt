-------Create Tables

CREATE OR REPLACE TABLE BIA.PUBLIC.PROJECT (
   PROJECTID NUMBER,   CLIENTID NUMBER,   PROJECT_NAME VARCHAR(255),   PROJECT_STATUS VARCHAR(255),
   START_DATE DATE,   END_DATE DATE,   PAID_SCAN_FREQUENCY NUMBER,   ORGANIC_SCAN_FREQUENCY NUMBER,
   SEM_RUSH_FREQUENCY NUMBER, NEW_SCAN_FREQUENCY NUMBER,   MEDIA_SCAN_FREQUENCY NUMBER,   SOCIAL_SCAN_FREQUENCY NUMBER
);


Create table BIA.PUBLIC.CLEARBIT( ID VARCHAR(255))


//Insert into BIA.PUBLIC.PROJECT (PROJECTID ,CLIENTID ,PROJECT_NAME,  PROJECT_STATUS ,PAID_SCAN_FREQUENCY,
//ORGANIC_SCAN_FREQUENCY,SEM_RUSH_FREQUENCY, NEW_SCAN_FREQUENCY,   MEDIA_SCAN_FREQUENCY,   SOCIAL_SCAN_FREQUENCY)
//Values(1,1,'BIA HPWSS','Test','Daily','Monthly','Monthly','Weekly','Monthly','Monthly')


CREATE OR REPLACE TABLE BIA.PUBLIC.COMPANY (
   COMPANYID NUMBER,   COMPANY_NAME VARCHAR(255), PROJECTID NUMBER
);


INSERT INTO BIA.PUBLIC.COMPANY (COMPANYID, COMPANY_NAME,PROJECTID)
VALUES  (1, 'Dell',1),   (2, 'Lenovo',1),   (3, 'HPE',1),   (4, 'ServiceNow',1),   (5, 'HPWSS',1),   (6, 'Xerox',1);


CREATE OR REPLACE TABLE BIA.PUBLIC.COMPANY_DOMAIN (
   COMPANYID NUMBER,PROJECTID NUMBER,   DOMAIN VARCHAR(255)
);


INSERT INTO BIA.PUBLIC.COMPANY_DOMAIN (COMPANYID,PROJECTID, DOMAIN)
VALUES  (1,1, 'dell.com'),   (2,1, 'lenovo.com'),   (3,1, 'hpe.com'),   (4,1, 'service-now.com'),
(4,1, 'servicenow.com'),   (5,1, 'hp.com'), (6,1, 'xerox.ca'),   (6,1, 'xerox.com') , (6,1,'xerox.de')


CREATE TABLE BIA.PUBLIC.SEARCHSCAPE_ORGANIC (
  DATE DATE,  REGION VARCHAR(255),  KEYWORD VARCHAR(255),  POSITION INT,
  TITLE VARCHAR(255),  LINK VARCHAR(255),  DOMAIN VARCHAR(255)
);

CREATE TABLE BIA.PUBLIC.SEARCHSCAPE_PAID (
  DATE DATE,  REGION VARCHAR(255),  KEYWORD VARCHAR(255),  POSITION INT,
  TITLE VARCHAR(255),  LINK VARCHAR(255),  DOMAIN VARCHAR(255)
);


CREATE TABLE SEARCHSCAPE_SEMRUSH (
  DATE DATE,  KEYWORD VARCHAR(255),  VOLUMN INT,  CPC FLOAT
);

CREATE OR REPLACE TABLE BIA.PUBLIC.NEWSSCAPE (
  DATE DATE,  DOMAIN VARCHAR(255),  REGION VARCHAR(255),  KEYWORD VARCHAR(255),  LINK VARCHAR(255),
  SOURCE_CODE_CNT INT,  TOTAL_WORD_CNT INT,  POSTIVE_WORD_CNT INT,  NEUTRAL_WORD_CNT INT,  NEGATIVE_WORD_CNT INT
);

CREATE OR REPLACE TABLE BIA.PUBLIC.SOCIALSCAPE_RAW (
  DATE DATE,  COMPANY_NAME VARCHAR(255),  REGION VARCHAR(255),  KEYWORD VARCHAR(255),  POST VARCHAR(255)
);


CREATE TABLE BIA.PUBLIC.SOCIALSCAPE (
  DATE DATE,  DOMAIN VARCHAR(255),  REGION VARCHAR(255),  KEYWORD VARCHAR(255),  LINK VARCHAR(255),
  SOURCE_CODE_CNT INT,  TOTAL_WORD_CNT INT, POSITIVE_WORD_CNT INT,  NEUTRAL_WORD_CNT INT,  NEGATIVE_WORD_CNT INT
);


select * from BIA.PUBLIC.KEYWORD
select * from BIA.PUBLIC.MEDIASCAPE

